# AI Agent Instructions

## Graph Generation Rules
- **MANDATORY VISUAL VERIFICATION:** Whenever you generate or modify an SVG/graph (e.g., using Asymptote), you MUST convert it to an image format (like PNG) and physically view it using your vision capabilities (e.g., `read_image_file` or `view_image`).
- **NEVER** mark a task as complete, say it is "ready," or use the submit tool until you have explicitly confirmed with your own "eyes" that the visual output matches the problem's visual layout, has no overlapping text, and is geometrically sound.
- **SEMANTIC UNDERSTANDING:** You must semantically understand the graphs and solve the underlying math to ensure coordinates are geometrically accurate before generating them.
